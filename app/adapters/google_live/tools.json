[
  {
    "name": "end_call",
    "description": "End the current call. Use this when the conversation is naturally complete and the customer's request has been fulfilled, or when the customer explicitly asks to end the call.",
    "parameters": {
      "type": "object",
      "properties": {
        "reason": {
          "type": "string",
          "description": "Brief reason for ending the call, e.g. 'booking_confirmed', 'inquiry_resolved', 'customer_requested_end'"
        }
      },
      "required": ["reason"]
    }
  },
  {
    "name": "transfer_to_agent",
    "description": "Transfer the caller to a human agent. Use this when the customer explicitly requests a human agent, or when you cannot resolve the issue.",
    "parameters": {
      "type": "object",
      "properties": {
        "reason": {
          "type": "string",
          "description": "Brief reason for the transfer, e.g. 'customer_requested', 'unable_to_resolve', 'complex_issue'"
        },
        "summary": {
          "type": "string",
          "description": "Summary of the conversation so far, including what the customer asked for and any actions taken. This helps the human agent pick up the conversation."
        }
      },
      "required": ["reason", "summary"]
    }
  },
  {
    "name": "execute_api",
    "description": "Make an HTTP request to the configured REST API. Use this tool to fetch data, create records, update records, or delete records based on the API documentation provided in your instructions. Construct the correct method, path, query parameters, and request body based on the API docs.",
    "parameters": {
      "type": "object",
      "properties": {
        "method": {
          "type": "string",
          "description": "HTTP method: GET, POST, PUT, DELETE, PATCH"
        },
        "path": {
          "type": "string",
          "description": "API path including path parameters, e.g. /items or /items/abc123"
        },
        "body": {
          "type": "string",
          "description": "JSON-encoded request body for POST/PUT/PATCH requests. Example: '{\"name\": \"Laptop\", \"price\": 999}'. Omit for GET/DELETE."
        },
        "query": {
          "type": "string",
          "description": "Query string parameters, e.g. 'limit=10&category=food'. Omit if not needed."
        }
      },
      "required": ["method", "path"]
    }
  }
]
